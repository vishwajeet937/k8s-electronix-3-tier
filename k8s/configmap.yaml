apiVersion: v1
kind: ConfigMap
metadata:
  name: electronix-config
  namespace: electronix

data:
  DB_HOST: "mysql-service"
  DB_PORT: "3306"
  DB_NAME: "electronix"
  CLIENT_URL: "http://frontend-service"
  NODE_ENV: "production"